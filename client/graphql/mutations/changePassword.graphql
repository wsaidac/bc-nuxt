mutation($oldPassword: String!, $password: String!, $passwordConfirmation: String!) {
  user: changePassword(oldPassword: $oldPassword, password: $password, passwordConfirmation: $passwordConfirmation) {
    id
  }
}
