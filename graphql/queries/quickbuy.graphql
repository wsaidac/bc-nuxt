#import "../fragments/productFields.graphql"

query {
  post: postBySlug(slug: "home") {
    ...on Home {
      quickbuy {
        quickbuyProduct {
          ...productFields
        }
      }
    }
  }
}
