query($slug: String) {
  menus(where: {slug: $slug}) {
    nodes {
      menuItems {
        nodes {
          label
          additionalAttributes {
            attachedImage {
              sourceUrl
            }
          }
          childItems {
            nodes {
              additionalAttributes {
                attachedImage {
                  sourceUrl
                }
              }
            }
          }
        }
      }
    }
  }
}
